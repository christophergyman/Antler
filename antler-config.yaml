github:
  repository: "christophergyman/claude-quick"  # Change this to your GitHub repository (e.g., "facebook/react")

server:
  host: "100.100.66.102"  # Your server's IP or hostname (e.g., Tailscale IP)
  backend_port: 8083
  frontend_port: 3000
